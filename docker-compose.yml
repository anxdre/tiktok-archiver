version: '3.8'

services:
  app:
    build: .
    container_name: tiktok-archiver-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
    volumes:
      - ./downloads:/app/downloads
      - ./archives:/app/archives
      - ./jobs:/app/jobs
      - ./storage:/app/storage
    restart: unless-stopped
    networks:
      - tiktok-network

  redis:
    image: redis:7-alpine
    container_name: tiktok-archiver-redis
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    networks:
      - tiktok-network
    command: redis-server --appendonly yes

networks:
  tiktok-network:
    driver: bridge

volumes:
  redis-data: